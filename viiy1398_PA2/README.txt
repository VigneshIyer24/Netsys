The server code is written for the TCP server and client is the web client

The code runs in the following way:

Given the file format for the one to be extracted, the code compares it with thegiven file formats and if available send to the web client.

The format for server is : ./makefile port-number

The URL format is : localhost:port-number/location


 
